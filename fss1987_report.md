# French, Schwert & Stambaugh（1987）再現分析レポート
## 「リスクが高まるとリターンも高まるか？」S&P500による実証

---

## 1. 論文の概要

### 出典

French, K. R., Schwert, G. W., & Stambaugh, R. F. (1987).  
"Expected stock returns and volatility."  
*Journal of Financial Economics*, 19(1), 3–29.

### 論文の問い

> **「予測可能なボラティリティが高い月は、リターンも高くなるか？」**

古典的な資産価格理論（CAPM）は「リスクが高ければリターンも高い」と予測する。しかしリスクが時間とともに変化するなら、**ボラティリティが高いと予測された月は、投資家がリスクプレミアムを要求し、リターンも高くなるはず**というのがこの論文の出発点である。

### 論文の主な発見

| 分析 | 結果 |
|---|---|
| 予測可能なボラティリティ → リターン | **正の関係**（ハイリスク・ハイリターンを支持） |
| 予測不可能なボラティリティ → リターン | **負の関係**（サプライズなボラティリティ上昇は株価を即座に押し下げる） |

2つの発見は矛盾しない。前者は「事前に予測できたリスクへの補償」、後者は「想定外のショックへの即時反応」という異なるメカニズムを捉えている。

---

## 2. 分析の枠組み

### 2.1 ボラティリティを「予測可能」と「予測不可能」に分ける理由

投資家がリターンへの上乗せを要求できるのは、**事前に認識できたリスク**に対してだけである。突発的なリスク（サプライズ）は投資前には知ることができないため、リターンへの事前の上乗せには影響しない。

そこで論文は月次ボラティリティを2つに分解し、それぞれがリターンに与える影響を別々に検証した。

```
月次実現分散（RV） = 予測可能な分散（σ²） ＋ 予測不可能な分散（u）
                      ↑                        ↑
              過去情報から予測できた部分       予測を外れたサプライズ
```

### 2.2 分析の全体フロー

```
Step 1　日次株価データの取得
         ↓
Step 2　日次対数リターンから月次実現分散（RV）を構築
         ↓
Step 3　ARIMAモデルで RV を「予測可能（σ²）」と「予測不可能（u）」に分解
         ↓
Step 4　2本の回帰を実行
         回帰①：σ² → 当期リターン（β の符号を確認）
         回帰②：u  → 当期リターン（γ の符号を確認）
         ↓
Step 5　係数の符号・p値から論文の主張を検証
```

---

## 3. 分析手法の詳細

### 3.1 月次実現分散（Realized Variance）の構築

論文の工夫として、月次の株価を1点だけ使うのではなく、**月内の全営業日の日次リターンを用いて**月次ボラティリティを構築する。これにより推定精度が大幅に向上する。

**計算式：**

$$RV_t = \sum_{d=1}^{D} r_d^2 + 2\sum_{d=2}^{D} r_d \cdot r_{d-1}$$

- $r_d$：日次対数リターン
- 第1項：日次リターンの2乗和（分散のメイン部分）
- 第2項：隣接日の積の和（日次リターンの自己相関を補正する項）

**数値例：**

```
1月の日次リターン：[+0.0106, -0.0148, +0.0085, -0.0064]

① 2乗和
   0.0106² + 0.0148² + 0.0085² + 0.0064²
 = 0.000112 + 0.000219 + 0.000072 + 0.000041 = 0.000444

② 隣接日補正
   2 × [(0.0106×-0.0148) + (-0.0148×0.0085) + (0.0085×-0.0064)]
 = 2 × [-0.000157 - 0.000126 - 0.000054] = -0.000674

③ RV_1月 = 0.000444 + (-0.000674) = -0.000230
   ※ 実際は何十営業日分もあるため正の値になります
```

**Pythonコード：**

```python
def calc_realized_variance(daily_returns):
    r = daily_returns.values
    squared_sum           = np.sum(r ** 2)           # ① 2乗和
    covariance_correction = 2 * np.sum(r[1:] * r[:-1])  # ② 隣接日補正
    return squared_sum + covariance_correction

monthly_rv = (
    daily
    .groupby(pd.Grouper(freq="ME"))  # 月ごとに仕切る
    .apply(calc_realized_variance)   # 各月に関数を適用
    .rename("RV")
)
```

---

### 3.2 ARIMAによる分解

月次実現分散 $RV_t$ に ARIMA(2, 0, 0)（= AR(2)モデル）を当てはめ、フィット値と残差に分解する。

**モデル式：**

$$RV_t = c + \phi_1 \cdot RV_{t-1} + \phi_2 \cdot RV_{t-2} + \varepsilon_t$$

- $\hat{RV}_t$（フィット値）= 予測可能な分散 $\sigma^2_t$
- $\varepsilon_t$（残差）= 予測不可能な分散 $u_t$

**数値例：**

```
月        実際のRV   予測値σ²     残差u
                    （予測可能）  （予測不可能）
2024-03   0.00254    0.00230      +0.00024
2024-04   0.00401    0.00260      +0.00141  ← 予想より大きかった
2024-05   0.00089    0.00310      -0.00221  ← 予想より小さかった
```

**Pythonコード：**

```python
arima_result = ARIMA(monthly_rv.dropna(), order=(2, 0, 0)).fit()

sigma2 = arima_result.fittedvalues  # 予測可能な分散（σ²）
u      = arima_result.resid         # 予測不可能な分散（u）
```

---

### 3.3 回帰分析

分解した2つの成分について、それぞれ当期リターンへの影響を OLS 回帰で推定する。

**回帰①：予測可能な分散 → 当期リターン**

$$R_t = \alpha + \beta \cdot \sigma^2_t + \varepsilon_t$$

- $\beta > 0$ かつ有意 → ハイリスク・ハイリターンを支持
- $\beta < 0$ または非有意 → 支持されない

**回帰②：予測不可能な分散 → 当期リターン**

$$R_t = \alpha + \gamma \cdot u_t + \varepsilon_t$$

- $\gamma < 0$ かつ有意 → ボラティリティのサプライズが即座に株価を押し下げる（ボラティリティフィードバック効果）

**Pythonコード：**

```python
# 回帰①
X1   = sm.add_constant(data["sigma2"])
reg1 = sm.OLS(data["Return"], X1).fit()

# 回帰②
X2   = sm.add_constant(data["u"])
reg2 = sm.OLS(data["Return"], X2).fit()
```

---

## 4. 分析結果（S&P500、1990〜2024年）

### 4.1 回帰①：予測可能な分散 → 当期リターン

```
Dep. Variable:    Return     R-squared:    0.007
No. Observations: 420        F-statistic:  3.135

             coef     p値
const        0.0111   0.001
sigma2      -1.9203   0.077
```

| 項目 | 値 | 解釈 |
|---|---|---|
| β（sigma2の係数） | −1.92 | 符号が負（論文の予測と逆） |
| p値 | 0.077 | 0.05を超えており非有意 |
| R² | 0.007 | 説明力はほぼない |

### 4.2 回帰②：予測不可能な分散 → 当期リターン

```
Dep. Variable:    Return     R-squared:    0.111
No. Observations: 420        F-statistic:  51.96

             coef     p値
const        0.0067   0.001
u           -4.6082   0.000
```

| 項目 | 値 | 解釈 |
|---|---|---|
| γ（u の係数） | −4.61 | 符号が負（論文の予測と一致） |
| p値 | 0.000 | 高度に有意 |
| R² | 0.111 | 回帰①より格段に高い説明力 |

### 4.3 結果サマリー

| | 係数 | p値 | FSS原論文との比較 |
|---|---|---|---|
| 回帰①（予測可能分散）β | −1.92 | 0.077（非有意） | 不一致（原論文は正） |
| 回帰②（予測不可能分散）γ | −4.61 | 0.000（有意） | 一致（原論文も負） |

---

## 5. 考察

### 5.1 回帰①の符号が逆になった理由

FSS(1987)の元論文（対象期間：1928〜1984年）では β > 0（正）が確認されたが、本分析（1990〜2024年）では β < 0（負）かつ非有意という結果になった。この背景として以下が考えられる。

- 本分析期間にはITバブル崩壊（2001年）、リーマンショック（2008年）、コロナショック（2020年）が含まれており、ボラティリティが高まった後にリターンが回復しないケースが多かった
- この結果は「低ボラティリティ・アノマリー」（Ang et al., 2006）とも整合的であり、高リスク株が必ずしも高リターンをもたらさないという近年の実証研究の流れと一致する

### 5.2 回帰②の強い結果の意味

γ = −4.61 は実質的にも大きい値である。予測不可能なボラティリティが 0.01（月次標準偏差で約1%相当）増加すると、月次リターンが約4.6%下落することを意味する。これは「ボラティリティのサプライズが株価を即座に押し下げる」というボラティリティフィードバック効果を強く支持している。

### 5.3 総括

1990〜2024年のS&P500では「予測可能なリスクが高いほどリターンが高い」という古典的なハイリスク・ハイリターン命題は統計的に支持されなかった。一方で「予期しないボラティリティの上昇はリターンを大きく押し下げる」という非対称な関係は強く確認された。リスクとリターンの関係は単純な比例関係ではなく、**リスクの予測可能性によって異なるメカニズムが働く**ことが示唆される。

---

## 6. 参考文献

- French, K. R., Schwert, G. W., & Stambaugh, R. F. (1987). Expected stock returns and volatility. *Journal of Financial Economics*, 19(1), 3–29.
- Ang, A., Hodrick, R. J., Xing, Y., & Zhang, X. (2006). The cross-section of volatility and expected returns. *Journal of Finance*, 61(1), 259–299.
- Engle, R. F., Lilien, D. M., & Robins, R. P. (1987). Estimating time varying risk premia in the term structure: The ARCH-M model. *Econometrica*, 55(2), 391–407.
