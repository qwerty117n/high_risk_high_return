# 第2章の数学付録：最適資産配分問題の解法

---

## A．3資産への最適資産配分問題の解法

> 本節では、安全資産および2個のリスク資産（株式・債券）を対象とする最適資産配分問題の解法を示す。

---

### 3資産を対象とする資産配分問題の定式化

#### 記号の定義

- $R_S$：株式のリターン
- $R_B$：債券のリターン
- $r_f$：安全資産のリターン（リスクフリーレート、確定値）
- $R_p$：ポートフォリオ全体のリターン
- $w_S$：株式への投資比率
- $w_B$：債券への投資比率
- $w_f$：安全資産への投資比率

投資家の目的関数は前節と同じ (2.21) 式：

$$
E(R_p) - \frac{\gamma}{2} \text{Var}(R_p), \quad \gamma > 0
$$

---

#### ポートフォリオリターンの整理

株式・債券・安全資産への投資比率をそれぞれ $w_S,\, w_B,\, w_f$ とすると、$R_p$ は：

$$
R_p = w_S R_S + w_B R_B + w_f r_f
\tag{2.35}
$$

制約条件 $w_S + w_B + w_f = 1$ より $w_f = 1 - w_S - w_B$ を代入すると：

$$
R_p = w_S(R_S - r_f) + w_B(R_B - r_f) + r_f
\tag{2.36}
$$

> **解説（式2.36の変形）：** $w_f r_f = (1 - w_S - w_B)r_f$ を展開して整理すると、各資産のリターンが「リスクフリーレートとの差（超過リターン）」の形で表せる。この形にすることで、以降の計算がシンプルになる。

---

#### 期待値と分散

(2.36) より、$R_p$ の期待値と分散は：

$$
E(R_p) = w_S\bigl(E(R_S) - r_f\bigr) + w_B\bigl(E(R_B) - r_f\bigr) + r_f
\tag{2.37}
$$

$$
\text{Var}(R_p) = w_S^2 \text{Var}(R_S) + w_B^2 \text{Var}(R_B) + 2w_S w_B \text{Cov}(R_S, R_B)
\tag{2.38}
$$

> **解説（式2.38について）：** 2資産の場合は $w_S^2 \text{Var}(R_S)$ だけだったが、3資産になると株式・債券間の**共分散（Cov）項**が加わる。この項が正（正の相関）なら合計リスクは増加し、負（負の相関）なら減少する。これが**分散投資の効果**の本質である。

---

#### 簡略表記の導入

表記を見やすくするために以下の記号を定義する：

$$
\mu_S \equiv E(R_S),\quad \mu_B \equiv E(R_B),\quad \sigma_S^2 \equiv \text{Var}(R_S),\quad \sigma_B^2 \equiv \text{Var}(R_B)
$$

$$
\sigma_{SB} \equiv \text{Cov}(R_S, R_B) = \rho\,\sigma_S\sigma_B \quad (\rho\text{：株式と債券の相関係数})
$$

これらを用いると、(2.37) 式は：

$$
E(R_p) = w_S(\mu_S - r_f) + w_B(\mu_B - r_f) + r_f
\tag{2.39}
$$

(2.38) 式は：

$$
\text{Var}(R_p) = w_S^2\sigma_S^2 + w_B^2\sigma_B^2 + 2w_S w_B \rho\sigma_S\sigma_B
\tag{2.40}
$$

---

#### 目的関数の展開

(2.39)・(2.40) を目的関数 (2.21) に代入すると：

$$
f(w_S, w_B) = w_S(\mu_S - r_f) + w_B(\mu_B - r_f) + r_f
- \frac{\gamma}{2}\!\left(w_S^2\sigma_S^2 + w_B^2\sigma_B^2 + 2w_S w_B \rho\sigma_S\sigma_B\right)
\tag{2.41}
$$

> **解説：** この (2.41) 式を最大にする $(w_S,\, w_B)$ を求めれば、株式・債券への最適な投資比率が得られる。安全資産への投資比率は $w_f = 1 - w_S - w_B$ から求まる。

---

### 3資産最適化問題の解法

$(w_S, w_B)$ が満たすべき制約条件はない（$w_f = 1 - w_S - w_B$ で自由に動ける）ので、$f(w_S, w_B)$ を $w_S$ と $w_B$ それぞれで**偏微分**してゼロとおき、連立方程式を解けばよい。

> **解説（偏微分とは）：** 多変数関数において、1つの変数だけを変化させたときの変化率を偏微分という。ここでは「$w_B$ を固定して $w_S$ だけ動かす」「$w_S$ を固定して $w_B$ だけ動かす」という2方向の最大化条件を同時に解く。

#### 1階条件

$$
\frac{\partial f(w_S, w_B)}{\partial w_S} = (\mu_S - r_f) - \gamma\!\left(\sigma_S^2 w_S + \rho\sigma_S\sigma_B w_B\right) = 0
\tag{2.42}
$$

$$
\frac{\partial f(w_S, w_B)}{\partial w_B} = (\mu_B - r_f) - \gamma\!\left(\sigma_B^2 w_B + \rho\sigma_S\sigma_B w_S\right) = 0
\tag{2.43}
$$

> **解説：** (2.42) は「株式のリスクプレミアムが、株式自身のリスクと債券との連動リスクの合計で説明される」という均衡条件を示している。(2.43) も同様に債券に関する条件である。

#### 最適解

(2.42)・(2.43) の連立1次方程式を解くと、答えは：

$$
w_S^* = \frac{1}{\gamma} \cdot \frac{\sigma_B^2(\mu_S - r_f) - \rho\sigma_S\sigma_B(\mu_B - r_f)}{\sigma_S^2\sigma_B^2(1 - \rho^2)}
\tag{2.44}
$$

$$
w_B^* = \frac{1}{\gamma} \cdot \frac{\sigma_S^2(\mu_B - r_f) - \rho\sigma_S\sigma_B(\mu_S - r_f)}{\sigma_S^2\sigma_B^2(1 - \rho^2)}
\tag{2.45}
$$

> **解説：** 2資産の最適解（式2.29）と同様に、$w_S^*$ と $w_B^*$ はいずれも $\gamma$（リスク回避度）に**反比例**している。つまり、リスク回避度の高い投資家は株式・債券への投資比率をともに下げ、その分だけ安全資産への投資比率を上げることが最適となる。

---

### 最適資産配分ルールの解釈

#### 株式と債券の相対比率

(2.44) と (2.45) の比をとって整理すると：

$$
\frac{w_S}{w_B} = \frac{\dfrac{\mu_S - r_f}{\sigma_S^2} - \rho\dfrac{\sigma_B}{\sigma_S}\cdot\dfrac{\mu_B - r_f}{\sigma_B^2}}{\dfrac{\mu_B - r_f}{\sigma_B^2} - \rho\dfrac{\sigma_S}{\sigma_B}\cdot\dfrac{\mu_S - r_f}{\sigma_S^2}}
\tag{2.46}
$$

特に、**相関係数がゼロ（$\rho = 0$）の場合**は：

$$
\frac{w_S}{w_B} = \frac{(\mu_S - r_f)/\sigma_S^2}{(\mu_B - r_f)/\sigma_B^2}
\tag{2.47}
$$

> **解説（式2.47の直感的な意味）：**
> - 分子：株式の「分散1単位あたりのリスクプレミアム」
> - 分母：債券の「分散1単位あたりのリスクプレミアム」
>
> つまり、相関がゼロのとき、**株式と債券の保有比率は、それぞれの「リスク効率（リスクプレミアム ÷ 分散）」に比例**させればよい。
>
> なお、(2.46) 式は相関がゼロでない場合でも、(2.47) 式に相関係数の大きさに応じた修正を加えれば対応できることを示している。

#### トービンの分離定理

(2.46) 式の右辺にも (2.47) 式の右辺にも、$\gamma$（リスク回避度）が含まれていない。

つまり、**株式と債券の相対比率は、投資家のリスク回避度とは独立**である。

> (2.46)・(2.47) 式が示すのは、**「リスク資産のポートフォリオをどう作るかは、投資家のリスク回避度に依存しない」** という命題である。これが**トービンの分離定理**である。
>
> 言い換えると：
> - リスク資産（株式・債券）の**相対比率**は、全投資家で共通
> - リスク回避度 $\gamma$ は、リスク資産全体と安全資産との**配分比率**にのみ影響する

---

## B．n資産への最適資産配分問題の定式化

> $n$ 個のリスク資産と安全資産を対象とする場合も、同じ枠組みで最適解を求めることができる。

---

### ポートフォリオの超過リターン

ポートフォリオの超過リターン $R'_p \,(\equiv R_p - r_f)$ は：

$$
R'_p = \sum_{i=1}^n w_i(R_i - r_f)
\tag{2.48}
$$

この $R'_p$ の期待値と分散は：

$$
E(R'_p) = \sum_{i=1}^n w_i\bigl(E(R_i) - r_f\bigr)
\tag{2.49}
$$

$$
\text{Var}(R'_p) = \sum_{i=1}^n\sum_{j=1}^n w_i w_j \text{Cov}(R_i, R_j)
= \sum_{i=1}^n w_i^2 \text{Var}(R_i) + 2\sum_{i=1}^n\sum_{j>i}^n w_i w_j \text{Cov}(R_i, R_j)
\tag{2.50}
$$

> **解説（式2.50について）：** $n$ 資産になると、共分散の項が $\binom{n}{2} = \frac{n(n-1)}{2}$ 個生まれる。たとえば $n=10$ なら45個もの共分散を考慮する必要があり、資産数が増えるほど計算は複雑になる。

---

### 簡略表記の導入

$$
\mu_i \equiv E(R_i),\quad \sigma_{ij} \equiv \text{Cov}(R_i, R_j)
\tag{2.51}
$$

（特に $i = j$ のとき $\sigma_{ii} = \text{Var}(R_i)$）

これを用いると：

$$
E(R'_p) = \sum_{i=1}^n w_i(\mu_i - r_f)
\tag{2.52}
$$

$$
\text{Var}(R'_p) = \sum_{i=1}^n\sum_{j=1}^n w_i w_j \sigma_{ij}
$$

---

### n資産の目的関数と最適解

目的関数は：

$$
f(w_1, w_2, \ldots, w_n) = E(R'_p) - \frac{\gamma}{2}\text{Var}(R'_p)
= \sum_{i=1}^n w_i(\mu_i - r_f) - \frac{\gamma}{2}\sum_{i=1}^n\sum_{j=1}^n w_i w_j \sigma_{ij}
\tag{2.53}
$$

この式を $w_1, w_2, \ldots, w_n$ について個々に**偏微分**してゼロとおくと、$n$ 個の連立1次方程式が得られる。この連立方程式を解けば最適配分比率が求まる。

> **解説：** ベクトルと行列を用いれば最適配分比率の公式を与えることができるが、ここでは割愛する。
>
> 重要なのは、**$n$ 資産に拡張しても、3資産の場合と本質的に同じ構造**を持つという点である。すなわち：
> - 最適な各資産への投資比率は $\gamma$（リスク回避度）に反比例する
> - リスク資産間の相対比率はリスク回避度に依存しない（トービンの分離定理は $n$ 資産でも成立する）

---

### 全体のまとめ

```
【3資産の最適化】
  目的関数 f(wS, wB) を wS・wB でそれぞれ偏微分してゼロとおく
  → 連立1次方程式 (2.42)(2.43) を解く
  → 最適解 wS* (2.44), wB* (2.45) を得る
  → wf* = 1 - wS* - wB*

【重要な含意：トービンの分離定理】
  株式・債券の相対比率 wS/wB は γ（リスク回避度）に依存しない
  γ はリスク資産全体 vs 安全資産の配分比率にのみ影響する

【n資産への拡張】
  超過リターン R'p を定義 → 期待値・分散を整理
  → 目的関数 (2.53) を n 個の wi で偏微分してゼロとおく
  → n 元連立1次方程式を解けば最適配分が求まる
```
