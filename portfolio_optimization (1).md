# 第2章 ポートフォリオ理論：2資産の最適化問題

---

## 4　2資産の最適化問題の解法

### 4.1　2資産を対象とする資産配分問題

#### 記号の定義

- $R_S$：株式のリターン（確率変数）
- $r_f$：安全資産のリターン（リスクフリーレート、確定値）
- $R_p$：ポートフォリオ全体のリターン
- $w_S$：株式への投資比率
- $w_f$：安全資産への投資比率
- $\gamma$：投資家のリスク回避度パラメータ（$\gamma > 0$）

---

#### 目的関数

投資家の目的関数を以下のように仮定する：

$$
E(R_p) - \frac{\gamma}{2} \text{Var}(R_p), \quad \text{ただし } \gamma > 0
$$

- $\gamma$ が大きい → リスクを強く嫌う → 安全資産寄りの配分
- $\gamma$ が小さい → リスクをあまり嫌わない → リスク資産寄りの配分

---

#### ポートフォリオのリターン・期待値・分散

$w_S$：株式への投資比率、$w_f$：安全資産への投資比率とすると、ポートフォリオリターンは下記のように計算される。

$R_p$ は：

$$
R_p = w_S R_S + w_f r_f
$$

これより、期待値と分散は：

$$
E(R_p) = w_S E(R_S) + w_f r_f
$$

$$
\text{Var}(R_p) = w_S^2 \text{Var}(R_S)
$$

---

### 4.2　2資産最適化問題の解法

上記を目的関数に代入すると：

$$
E(R_S) w_S + r_f w_f - \frac{\gamma}{2} \text{Var}(R_S) w_S^2
$$

制約条件 $w_S + w_f = 1$ より $w_f = 1 - w_S$ を代入すると：

$$
E(R_S) w_S + r_f (1 - w_S) - \frac{\gamma}{2} \text{Var}(R_S) w_S^2 \quad (\because w_f = 1 - w_S)
$$

整理すると、$w_S$ だけの関数 $f(w_S)$ として表せる：

$$
f(w_S) = \bigl(E(R_S) - r_f\bigr) w_S - \frac{\gamma}{2} \text{Var}(R_S) w_S^2 + r_f
$$

$f(w_S)$ を最大にする $w_S$ を求めるには、$f'(w_S) = 0$ を解けばよい：

$$
f'(w_S) = \bigl(E(R_S) - r_f\bigr) - \gamma \text{Var}(R_S) w_S = 0
$$

これを $w_S$ について解くと、最適な株式投資比率は：

$$
\boxed{w_S^* = \frac{1}{\gamma} \cdot \frac{E(R_S) - r_f}{\text{Var}(R_S)}}
$$

$E(R_S) = \mu_S$、$\text{Var}(R_S) = \sigma_S^2$ と表記すると：

$$
w_S^* = \frac{1}{\gamma} \cdot \frac{\mu_S - r_f}{\sigma_S^2}
$$

---

### 4.3　最適資産配分のルールの解釈

$(\mu_S - r_f)$ は**リスクプレミアム**：リスク資産の期待リターンとリスクフリーレートの差。

- リスクプレミアムが高いほど　　→　リスク資産に対する投資比率が高まる
- リスク資産の分散が小さいほど　→　リスク資産に対する投資比率が高まる
- リスク回避度が高いほど　　　　→　リスク資産に対する投資比率が低くなる
